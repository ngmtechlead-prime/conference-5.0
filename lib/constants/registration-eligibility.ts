import "server-only";

/**
 * Populate only with verified, normalized sponsored-attendee email addresses.
 * Eligibility enforcement must remain disabled while this list is empty.
 */
export const SPONSORED_ATTENDEE_EMAILS: readonly string[] = [];
